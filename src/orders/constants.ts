export enum OrderType {
  BUY = 'buy',
  SELL = 'sell',
}

export enum OrderSource {
  WHATSAPP = 'whatsapp',
  WEB = 'web',
  API = 'api',
}

export enum OrderStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  FAILED = 'failed',
}
